{
  "name": "gminimailhater",
  "version": "1.0.0",
  "type": "module",
  "author": "Alynva",
  "description": "Automatically get rid of the minimail notification.",
  "license": "ISC",
  "scripts": {
    "start": "node dist/index.mjs",
    "dev": "nodemon src/index.mjs -p 9092",
    "build": "npx esbuild src/index.mjs --bundle --external:gnode-api --minify --platform=node --outfile=dist/index.mjs --format=esm"
  },
  "keywords": [],
  "dependencies": {
    "gnode-api": "^0.2.18"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
